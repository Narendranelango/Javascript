document.body.addEventListener('keyup',function(ob){//We use 'ob' get the value in the main-id
  console.log("Key Up")
})

document.body.addEventListener('keydown',function(ob){//We use 'ob' get the value in the main-id
    console.log("Key Down")
  })

document.body.addEventListener('keypress',function(ob){//We use 'ob' get the value in the main-id
    console.log("Key Press")
  })